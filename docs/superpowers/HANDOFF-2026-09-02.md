# TEN Scraper — Session Handoff (2026-09-02)

Work done autonomously while you were away. Everything is on the git branch
**`feature/scraper-phase1`** (local repo, no remote). Nothing was deployed and
the live DB was **not** touched — those steps need you.

## What's done

| Area | Status |
|---|---|
| Design spec | ✅ `docs/superpowers/specs/2026-09-02-scraper-system-design.md` |
| Phase 1 (foundation) code | ✅ written, **not yet run/deployed** (needs your DB + WinSCP) |
| Phase 3 (Python worker) | ✅ built **and unit-tested locally — 29 tests passing** |
| Worker server adapters + CLI | ✅ written, syntax-checked (server-verified later) |
| Phase 1 & 3 plans | ✅ under `docs/superpowers/plans/` |
| Phase 2 plan (config UI) | ✅ `docs/superpowers/plans/2026-09-02-scraper-phase2-config-ui.md` |

## YOUR STEPS when you return (Phase 1 — makes the module appear)

**1. Apply the schema to the live `TEN_Management` DB** (phpMyAdmin → SQL tab):
- Paste the contents of `scraper/schema.sql` → Go.
- Paste the contents of `scraper/install_module.sql` → Go.

**2. Verify (run in SQL tab):**
```sql
SELECT COUNT(*) FROM information_schema.tables
WHERE table_schema='TEN_Management' AND table_name LIKE 'ten_scraper_%';   -- expect 8
SELECT id,name,type FROM ten_scraper_projects;                              -- expect 2 rows
SELECT module_key,module_url,required_permission FROM ten_modules WHERE module_key='scraper';
```

**3. Deploy to staging** via WinSCP FTPS CLI to `/management-staging/`:
- `module-scraper.php`
- `scraper/lib/scraper_db.php`
- `config.php`  *(now has 3 new empty keys: OPENAI/PEXELS/UNSPLASH — harmless)*

**4. Open** `…/management-staging/module-scraper.php` while logged in.
Expect: sidebar + header, a "News Scraper" heading, and two tabs — **News
Collation** and **Cafe/Restaurant Email Collection**. A "News Scraper" link
should also appear in the sidebar's **Content** group.

If all that looks right, Phase 1 is verified and we can build Phase 2 (the
config UI) against staging.

## Python worker (Phase 3) — already verified here

- `scraper/worker/` — run tests any time:
  ```
  cd scraper/worker && venv/Scripts/python.exe -m pytest -q
  ```
- 29 tests green: URL dedup, story clustering, robots/rate-limit politeness,
  RSS/Atom parsing, HTML fact-extraction, VPN egress control, ingest dedup.
- It does **not** run on the server yet — that's Phase 4 (installing deps incl.
  optional `trafilatura`, the master cron, and the VPN network-namespace setup).

## Security reminders

- **Rotate the OpenAI key** you pasted in chat, then put the fresh one in
  `config.php` (`OPENAI_API_KEY`). The key is not stored in the repo.
- `config.php` / `config_ten_admin.php` / `*.sql` dumps are gitignored — secrets
  never entered version control.

## Branch

Work is on `feature/scraper-phase1`, branched from `master`. When you're happy,
we merge to `master`. Ask me and I'll walk through it (or do it).
